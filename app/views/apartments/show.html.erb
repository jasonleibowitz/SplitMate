<%= render @apartment %>

<%= link_to 'Add Roomates', "/apartments/#{params[:id]}/addroommates" %>

<%= link_to 'Create a chore', new_apartment_chore_path(@apartment) %>


<h1>Chore Marketplace</h1>

<%= render :partial => '/chores/chores_index' %>

<h2>Assigned Chores</h2>

<%= render :partial => '/chores/assigned_chores_index' %>

<h2>Leaderboard</h2>

<%= render 'leaderboard' %>
